

# 🤖 Voice Assistant for Small Business (Windows)

Голосовой ассистент для малого бизнеса, полностью работающий на Windows. Он принимает звонки через Voximplant, распознаёт речь с помощью Yandex SpeechKit, синтезирует голос и отвечает, используя OpenAI GPT-4o-mini. Ассистент умеет вести диалог с контекстом по номеру телефона, использовать заранее подготовленную базу ответов из файла faq.json и применять системный промт из файла prompt.txt. Если ответа на вопрос нет в базе, ассистент использует промт и обращается к ChatGPT для генерации подходящего ответа.

Ассистент подходит для магазинов, салонов, сервисов, клиник и других компаний, где нужно автоматизировать приём звонков и ответы на типовые вопросы.



# Требования

Windows 10 или 11  
Python 3.10+  
Node.js 18+  
Аккаунты: Voximplant, Yandex Cloud (SpeechKit), OpenAI


# Установка и настройка

Сначала зарегистрируйся и получи ключи доступа к API.

Зайди на сайт https://voximplant.com и создай аккаунт. В панели управления создай приложение и сценарий, который отправляет запись звонка на HTTP webhook (наш сервер). Сохрани данные Account ID и Application name.

Перейди в Yandex Cloud https://cloud.yandex.ru/services/speechkit, создай каталог и сервисный аккаунт, включи API SpeechKit, создай IAM-токен и скопируй его — он понадобится в переменной `YANDEX_API_KEY`.

На сайте OpenAI https://platform.openai.com/account/api-keys создай новый API-ключ и скопируй его — он понадобится для переменной `OPENAI_API_KEY`.


# Настройка окружения

Создай виртуальное окружение и установи зависимости.  
В консоли Windows выполни:

```

python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt

```

Создай файл `.env` в корне проекта и добавь в него:

```

OPENAI_API_KEY=sk-ваш_ключ_OpenAI
YANDEX_API_KEY=ваш_IAM_токен_Yandex
VOX_SIGNATURE_SECRET=
BASE_URL=[https://example.loca.lt](https://example.loca.lt)
FAQ_PATH=faq.json
PROMPT_PATH=prompt.txt

```

---

# Создание файлов FAQ и Prompt

Создай файл `faq.json` в корне проекта и добавь в него список частых вопросов в формате JSON:

```

[
 {
  "question": "какие часы работы",
  "answer": "Мы работаем каждый день с 9:00 до 20:00."
 },

 {
  "question": "где вы находитесь",
  "answer": "Мы находимся по адресу улица Ленина, дом 15."
 },

 {
  "question": "как сделать заказ",
  "answer": "Вы можете сделать заказ по телефону или через наш сайт."
 }
]

```

Ассистент будет искать совпадения по ключевым словам в вопросе. Если не найдёт — обращается к GPT-4o-mini.

Создай файл `prompt.txt` в той же папке. Пример содержимого:

```

Вы — дружелюбный и профессиональный голосовой ассистент компании. Отвечайте вежливо, кратко и по делу, не отклоняясь от темы. Если информации нет, предложите клиенту связаться с оператором.

```

Этот текст будет использоваться как системный промт при генерации ответов с помощью GPT.


# Настройка публичного адреса для Voximplant

Чтобы Voximplant мог отправлять вебхуки с аудио звонков на ваш локальный сервер, нужен публичный адрес. Есть два варианта настройки:

Если ваш IP **заблокирован или нет прямого доступа**, используйте LocalTunnel. LocalTunnel создаёт безопасный публичный URL, который перенаправляет запросы на ваш локальный сервер.

1. Установите Node.js с [официального сайта](https://nodejs.org). Глобальная установка достаточна, не нужно ставить в папку проекта.
2. В терминале проекта выполните команду:
```

npx localtunnel --port 8000

```
3. После запуска появится строка вида:
```

your url is: [https://ancient-wolf-53.loca.lt](https://ancient-wolf-53.loca.lt)

```
4. Скопируйте этот URL и вставьте его в файл `.env` вместо `BASE_URL`:
```

BASE_URL=[https://ancient-wolf-53.loca.lt](https://ancient-wolf-53.loca.lt)

```
Теперь Voximplant сможет достучаться до вашего сервера даже через NAT или заблокированный IP.

Если ваш IP **не заблокирован и доступен извне**, можно использовать прямой публичный IP без туннеля:

1. В `.env` укажите ваш публичный IP и порт сервера:
```

BASE_URL=http://<ваш-публичный-ip>:8000

```
2. Убедитесь, что на роутере или фаерволе разрешены входящие соединения на порт 8000.  
3. Voximplant сможет обращаться напрямую, без LocalTunnel.

> Резюме: используйте LocalTunnel для временного публичного адреса или если IP заблокирован. Если есть прямой доступ к публичному IP, используйте его для стабильной работы сервера.



# Запуск проекта

Активируй виртуальное окружение:
```

venv\Scripts\activate

```

Запусти сервер:
```

python main.py

```

В другой консоли запусти туннель:
```

npx localtunnel --port 8000

```

После этого сервер станет доступен по внешнему адресу, например https://ancient-wolf-53.loca.lt.


# Настройка Voximplant

После того как у вас есть публичный адрес (через LocalTunnel или прямой публичный IP), настройте webhook в Voximplant, чтобы звонки приходили на ваш сервер.

1. Зайдите в Voximplant → Applications → HTTP Requests → Webhooks.  
2. Добавьте webhook с методом **POST** на ваш публичный URL и путь `/vox/webhook`.  
  - Если используете LocalTunnel, пример:
```

POST [https://ancient-wolf-53.loca.lt/vox/webhook](https://ancient-wolf-53.loca.lt/vox/webhook)

```
- Если используете прямой публичный IP, пример:
```

POST http://<ваш-публичный-ip>:8000/vox/webhook

```
3. Сохраните настройки.  

Теперь при звонке Voximplant будет отправлять аудио на ваш сервер. Ассистент распознаёт речь, формирует ответ (с использованием FAQ или GPT-4o-mini) и возвращает озвученный результат для проигрывания звонящему.




# Как работает система

1. Клиент звонит на номер компании, подключённый к Voximplant.  
2. Voximplant записывает речь и отправляет аудио на вебхук.  
3. Сервер принимает аудио, отправляет его в Yandex SpeechKit STT для распознавания текста.  
4. Сервер проверяет, есть ли вопрос в базе faq.json.  
5. Если совпадение найдено — ответ берётся из базы.  
6. Если нет — используется системный промт из prompt.txt и запрос идёт к GPT-4o-mini.  
7. Полученный ответ синтезируется в голос через Yandex SpeechKit TTS.  
8. Голос отправляется обратно в Voximplant для проигрывания звонящему.  
9. Контекст разговора сохраняется по номеру телефона на время звонка.



# Структура проекта

```

voice-assistant/
│
├── main.py                # основной сервер FastAPI: маршруты, webhook Voximplant
├── requirements.txt       # список зависимостей 
├── .env                   # переменные окружения (API ключи, BASE_URL, секреты)
├── faq.json               # база FAQ с вопросами и ответами
├── prompt.txt             # промт для GPT-4o-mini
├── run.bat                # батник для запуска сервера на Windows
├── README.md              # инструкции по установке, запуску, LocalTunnel/публичный IP, Voximplant
└──static/                # папка для сохранения сгенерированных mp3 через Yandex TTS
    └── *.mp3              # файлы озвучки
```

# Возможные ошибки и решения

SpeechKit 401 Unauthorized — проверь токен в `YANDEX_API_KEY`.  
OpenAI AuthenticationError — проверь `OPENAI_API_KEY`.  
LocalTunnel session error — просто перезапусти туннель.  
FAQ not found — добавь вопрос в faq.json.  
Connection refused — сервер не запущен.  
500 Internal Server Error — смотри логи в консоли.


# Советы по использованию

Меняй содержимое prompt.txt, чтобы контролировать стиль общения ассистента.  
Регулярно обновляй faq.json, чтобы ускорять ответы без обращения к GPT.  
Для продакшена используй VPS с постоянным доменом.  
Добавь логирование разговоров для анализа вопросов клиентов.  
Можно подключить CRM, чтобы ассистент узнавал клиента по номеру.


# Краткий чек-лист запуска

Установи Python и Node.js, создай виртуальное окружение, установи зависимости, создай файлы `.env`, `faq.json`, `prompt.txt`, запусти `python main.py`, подними туннель `npx localtunnel --port 8000`, настрой webhook в Voximplant и сделай тестовый звонок. Ассистент ответит голосом.
